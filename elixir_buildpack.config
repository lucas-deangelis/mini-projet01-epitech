# Erlang version
erlang_version=21.0

# Elixir version
elixir_version=1.9

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Set the path the app is run from
runtime_path=/app

# A command to run before fetching dependancies
hook_pre_fetch_dependencies="mix deps.get"